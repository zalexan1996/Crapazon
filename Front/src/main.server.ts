import { bootstrapApplication } from '@angular/platform-browser';
import { AppRootComponent } from './app/app-root/app-root.component';


const bootstrap = () => bootstrapApplication(AppRootComponent)

export default bootstrap;
